---
ticket_id: TKT-20260921-000017-001
state: READY
priority: high
owning_module: events
created: 2026-09-21
updated: 2026-09-21
parent_ticket: TKT-20260921-000016-001
---

# Ticket: Event Management and Participation Approval Vertical Slice

## Implementation Owner

Developer B — Chandler Lovely (GitHub: ChanMan64)

## Objective

Deliver a usable MVP Event-management vertical slice on top of Tickets 15 and
16. An authorized Event manager must be able to operate an Event through the
application: manage ordinary Event information, administer Event managers when
owner-authorized, review Unit participation requests, approve or deny pending
requests, and observe the resulting participation state. Protected operations
must remain unavailable to unauthorized Users, while existing public Event
browsing remains functional.

This is an application workflow, not only a collection of server functions.
Build forward from the current local Prisma schema and migrations. Do not
implement the feature before this ticket is assigned for implementation.

## Main Success Scenario

1. An Event exists with an owner and, optionally, explicit Event managers.
2. The owner or an explicit manager opens an Event-management workflow and can
   perform ordinary allowed Event management.
3. The owner can add or revoke explicit Event managers; explicit managers cannot
   administer other managers.
4. A Unit submits a participation request through the existing Ticket 15 path.
5. An Event manager sees the pending request and approves or denies it.
6. The resulting participation state is visible through the appropriate
   management/read-side surfaces, without exposing pending or denied management
   information through public browsing.
7. Unauthorized Users are denied every protected operation server-side.

## Required Product Outcomes

### Event Management

- An Event owner or explicit Event manager can perform ordinary MVP Event
  management through the existing `canManageEvent(userId, eventId)` boundary.
- Existing Event editing behavior is usable through the application and
  current Event timing/lifecycle rules are preserved.
- If Event creation already has UI, integrate it without disrupting it. If it
  exists only server-side and minimal UI work is required for a usable Events
  workflow, that work is GREEN within Events.
- Do not redesign unrelated public Event browsing.

### Event Authorized-User Administration

- The owner can add an existing website User as an explicit Event manager.
- The owner can revoke an explicit Event manager.
- Duplicate Event/User authorization is rejected safely.
- The owner cannot receive a duplicate explicit manager row.
- An explicit manager cannot add or revoke other managers.
- Ownership transfer is not supported.
- Manager self-removal is optional only if Ticket 16 already supports it or it
  follows naturally without broadening manager-administration authority.

### Participation Review and Decision

- Event managers can inspect EventParticipation records for Events they can
  manage, with `REQUESTED` records visible and actionable.
- The owner or explicit Event manager can approve a `REQUESTED` participation.
- The owner or explicit Event manager can deny a `REQUESTED` participation.
- Authorization for both decisions uses `canManageEvent(userId, eventId)`.
- Valid lifecycle transitions remain only:
  `REQUESTED -> APPROVED` and `REQUESTED -> DENIED`.
- Invalid or terminal transitions fail safely. Do not add approval reversal,
  denial reversal, re-request/reset behavior, or duplicate EventParticipation
  rows without a later RED decision.

### Read-Side and UI

Provide the minimum usable interface, using existing application conventions,
so an authorized manager can:

- identify Events they can manage;
- open and manage an Event;
- inspect relevant Event information and explicit Event managers;
- review pending participation requests;
- approve or deny pending requests; and
- observe the resulting participation state.

The owner must additionally be able to add and revoke Event managers through
the UI. Exact routes, components, layouts, forms, and interaction design are
Developer B's GREEN responsibility.

### Public and Privacy Behavior

- Preserve existing public Event browsing and approved participation behavior
  where consistent with the existing Event read-side.
- Do not expose pending or denied management information publicly merely because
  the management interface needs it.
- Make reasonable MVP presentation decisions within Events; escalate only when
  a genuine product or privacy boundary must change.

## Approved Architecture Boundaries

### Existing Event Management

Every protected ordinary Event-management operation must use the existing
server-only semantic capability:

```ts
canManageEvent(userId, eventId)
```

Do not reconstruct Event authorization downstream or inspect Unit permissions,
`PermissionGrant`, `PermissionScope`, `RootUnit`, `Commander`,
`authorityLevel`, delegation lineage, or participating-Unit authority.

### Event Manager Administration

Use the Ticket 16 approved owner-only Event-manager administration boundary.
`canManageEvent` is not permission to administer the manager list. The owner
may add and revoke explicit managers; explicit managers may not administer
other managers.

### Participation Requests

Unit-side request creation remains owned by Ticket 15 and continues to use:

```ts
canRequestEventParticipation(userId, unitId)
```

Do not modify that authorization model. Developer B owns Event-side request
review and decision behavior and must use `canManageEvent` for approval/denial.
Participation grants no Event authority.

### Persistence Baseline

Before changing persistence, inspect the actual current Prisma schema and
migrations. Preserve `EventParticipation`, Event ownership, and
`EventAuthorizedUser`, as well as Units authority and Auth.js data. Build
forward only if a genuine in-scope persistence adjustment is required; do not
create redundant authority or participation state, reset the database, or
rebuild migrations for convenience.

## Developer Ownership / Escalation

Developer B owns ordinary GREEN decisions inside the Events boundary,
including:

- service, query, and helper organization;
- server-action or API organization;
- transaction design;
- route, page, and component structure;
- form and interaction design;
- Event-management and participation-management UI;
- validation and error representation;
- test organization;
- ordinary Events refactoring; and
- indexes or query optimizations that do not change shared semantics.

Inside the Events box, implement freely; at an approved boundary, obey the
contract; if the boundary must change, escalate. If one RED issue blocks only
part of the feature, continue independent in-scope work where possible.

Escalate only if implementation requires a RED decision, including:

- changing `canManageEvent` semantics;
- changing owner-only Event-manager administration;
- changing `MANAGE_EVENTS` semantics;
- changing Event ownership or adding ownership transfer;
- adding Unit ownership or Unit authority anchoring to Event;
- changing EventParticipation lifecycle semantics;
- allowing participation retry/reset or terminal-state reversal;
- introducing a new cross-module dependency or exposing Units authority
  internals;
- changing Ticket 15 or Ticket 16 architectural invariants; or
- beginning Audit architecture, persistence, or workflow work.

Do not preemptively block the ticket for a hypothetical RED decision.

## Scope

```yaml
scope:
  read:
    - AGENT_WORKFLOW.md
    - docs/DEVELOPMENT_STANDARD.md
    - docs/SYSTEM.md
    - docs/architecture.md
    - BACKLOG.md
    - docs/registry/MODULES.yaml
    - docs/registry/CONTRACTS.yaml
    - docs/registry/CAPABILITIES.yaml
    - docs/decisions/ADR-20260921-004.md
    - docs/bcr/BCR-20260921-002.md
    - docs/contracts/events-units-event-creation-authorization.md
    - src/modules/events/MODULE.md
    - src/modules/events/server/**
    - src/modules/units/MODULE.md
    - src/modules/units/server/authorization.ts
    - src/auth.ts
    - src/lib/prisma.ts
    - prisma/schema.prisma
    - prisma/migrations/**
    - tickets/OPEN/**
    - tickets/COMPLETED/2026/09/TKT-20260921-000015-001.md
    - tickets/COMPLETED/2026/09/TKT-20260921-000016-001.md
  write:
    - tickets/OPEN/TKT-20260921-000017-001.md
    - src/modules/events/**
    - src/app/events/**
    - prisma/schema.prisma
    - prisma/migrations/**
    - src/modules/events/MODULE.md
    - src/modules/events/CHANGELOG.md
    - docs/registry/MODULES.yaml
    - docs/registry/CONTRACTS.yaml
    - docs/registry/CAPABILITIES.yaml
    - Events-owned tests under src/modules/events/**
```

Write access to Prisma and registries is conditional: use it only for a
necessary in-scope adjustment or implementation-status update. Units and
Players implementation files are not in write scope. Ticket 14 remains owned
by Developer A and must remain untouched. Events may consume Units capabilities,
but Units implementation remains read-only.

## Context Ledger

### Modules

- Declared: [events, units, shared-authentication]
- Discovered: [events, units, players boundary, shared Prisma]
- Modified: []
- Created: [Event-management and participation-approval vertical slice, pending implementation]
- Externally modified: [Ticket 14 Players work]

### Contracts

- Declared: [canManageEvent, owner-only Event-manager administration,
  canRequestEventParticipation]
- Discovered: [events-units-event-creation-authorization]
- Modified: []
- Created: []

### Capabilities

- Declared: [canManageEvent, owner-only Event-manager administration]
- Discovered: [canRequestEventParticipation, canCreateEvent]

### Decisions

- Referenced: [ADR-20260921-004, BCR-20260921-002]
- Created: []

### Context Discoveries

| Artifact | Type | Reason discovered | Scope/log entry |
|---|---|---|---|
| tickets/COMPLETED/2026/09/TKT-20260921-000015-001.md | completed ticket | EventParticipation persistence, request authorization, and lifecycle baseline | 2026-09-21 / declared |
| tickets/COMPLETED/2026/09/TKT-20260921-000016-001.md | completed ticket | Event ownership, explicit managers, and approved management boundaries | 2026-09-21 / declared |
| src/modules/events/MODULE.md | module boundary | Events owns Event identity, management capability, and manager administration | 2026-09-21 / declared |
| src/modules/units/server/authorization.ts | capability provider | Unit-side participation request capability remains outside Events | 2026-09-21 / declared |
| prisma/schema.prisma | persistence source | Current Event, EventParticipation, and EventAuthorizedUser truth | 2026-09-21 / declared |
| tickets/OPEN/TKT-20260921-000014-001.md | neighboring ticket | Players work is Developer A-owned and must remain untouched | 2026-09-21 / conflict review |

## Assumptions

| Statement | Status | Evidence or decision |
|---|---|---|
| Tickets 15 and 16 are part of the current local schema and capability baseline. | CONFIRMED | Completed tickets and current repository state. |
| Event management means Event owner or explicit Event manager, not Unit authority. | CONFIRMED | Ticket 16, ADR-20260921-004, and BCR-20260921-002. |
| Only the owner administers explicit Event managers. | CONFIRMED | Ticket 16 approved invariant. |
| EventParticipation has one unique Event/Unit record with REQUESTED, APPROVED, and DENIED states. | CONFIRMED | Ticket 15 approved persistence and lifecycle. |
| Manager self-removal is optional for MVP. | CONFIRMED | Optional only when naturally supported by the Ticket 16 implementation. |
| New persistence is required for this slice. | ACCEPTED-AS-LIMITATION | Inspect current schema first; no new persistence is expected unless a genuine need is discovered. |

## Acceptance Criteria

- [ ] Event owner can manage their Event.
- [ ] Explicit Event manager can manage the Event.
- [ ] Unrelated User cannot manage the Event.
- [ ] `MANAGE_EVENTS` alone does not allow management of another User's Event.
- [ ] Event owner can add an Event manager.
- [ ] Event owner can revoke an Event manager.
- [ ] Explicit Event manager cannot administer other managers.
- [ ] Duplicate Event manager authorization is rejected.
- [ ] Owner cannot be added as a duplicate manager.
- [ ] Revoked manager loses management access.
- [ ] Event manager can view pending participation requests.
- [ ] Event manager can approve REQUESTED participation.
- [ ] Event manager can deny REQUESTED participation.
- [ ] Unauthorized User cannot approve or deny participation.
- [ ] Invalid or terminal participation transitions are rejected.
- [ ] Ticket 15's unique Event/Unit participation invariant remains intact.
- [ ] Cross-RootUnit participation remains valid.
- [ ] Participation itself grants no Event authority.
- [ ] Event-management workflow is usable through the application UI.
- [ ] Owner manager-administration workflow is usable through the UI.
- [ ] Existing public Events behavior remains functional.
- [ ] Ticket 13 authorization regressions remain green.
- [ ] Ticket 15 EventParticipation regressions remain green.
- [ ] Ticket 16 Event authorization regressions remain green.
- [ ] Focused Event-management, manager-administration, and participation-
      decision tests pass.
- [ ] Prisma validation, generation, and migration deploy/status checks pass
      where applicable.
- [ ] Lint, type-check, production build, and `git diff --check` pass.
- [ ] Actual verification results are recorded before the ticket is completed.

## Verification

- Focused Event-management authorization and ordinary management tests.
- Focused Event manager add/revoke, duplicate, owner, and revocation tests.
- Focused EventParticipation request review and approval/denial tests.
- Ticket 13 authorization regression suite.
- Ticket 15 EventParticipation regression suite.
- Ticket 16 Event authorization regression suite.
- Prisma validation/generation and migration deploy/status where applicable.
- Lint, type-check, production build, and `git diff --check`.

Expected result: all focused behavior, regression, schema, and quality checks
pass, with actual commands and results recorded in this ticket before
completion.

## Explicitly Out of Scope

- Audits, Audit persistence, Audit submission, or Audit approval/denial UI.
- Player K/D/A, Unit audit statistics, advanced statistics, or leaderboards.
- Event ownership transfer.
- Unit hierarchy inheritance for Event management.
- Ancestor `MANAGE_EVENTS` oversight over existing Events.
- Event-to-Unit ownership or authority.
- Generalized RBAC.
- Players/Rosters work from Ticket 14.
- Changes to Ticket 15 request authorization or Ticket 16 authority semantics.
- Public Event redesign unrelated to making the management workflow usable.

## Boundary and Conflict Review

- Shared artifacts checked: Tickets 15 and 16, Events and Units module
  documentation, approved Event authorization ADR/BCR/contract, Prisma schema
  and migrations, and open Ticket 14.
- Boundary change required: no; this ticket consumes approved boundaries.
- BCR: BCR-20260921-002 referenced; no new BCR required to begin.
- Scope-growth review: Events-only implementation and UI integration; Units
  and Players implementations remain read-only.

## Completion Record

Status: READY
Completed: N/A
Implementation commit: N/A
Pull request: N/A

### Result

[Record the completed Event-management and participation-approval outcome.]

### Files Changed

- [paths recorded at completion]

### Documentation Updated

- [paths recorded at completion]

### Registries, Changelogs, and Decisions

- Registries: [updated/not applicable]
- Changelogs: [updated/not applicable]
- BCRs: [reference/not applicable]
- ADRs: [reference/not applicable]

### Deviations and Known Limitations

- [none or details]
