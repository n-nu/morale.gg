---
ticket_id: TKT-20260914-000005-001
state: READY
priority: high
owning_module: events
created: 2026-09-14
updated: 2026-09-14
parent_ticket: null
---

# Ticket: Establish Events Module and Public Event Browsing

## Implementation Owner

Developer B <- replace your name here

## Objective

Establish the initial Events product module, introduce the minimal Event
persistence required by the MVP, and implement public read-only Event
browsing.

This ticket establishes the Event concept before EventParticipation,
authorization, and Audits are introduced.

## New-Module Exception

This is the first Events ticket. It explicitly authorizes creation of the
new `events` module, so the absence of `src/modules/events/MODULE.md` is not
a blocking condition under `AGENT_WORKFLOW.md` Section 5. Developer B must:

1. Read `docs/templates/MODULE_TEMPLATE.md`.
2. Define the module's purpose, ownership, non-ownership, and public boundary.
3. Create `src/modules/events/MODULE.md` from the canonical template.
4. Register the Events module in `docs/registry/MODULES.yaml`.
5. Create only contracts justified by an actual cross-module consumer.

## Scope Owner

```yaml
scope_owner:
  type: module
  id: events
```

Rationale: this ticket establishes the Events module, its minimal approved
Event persistence, and its public read-side presentation. The new-module
exception applies because an Events `MODULE.md` does not yet exist.

## Human-Approved Event Persistence Scope

Human architectural authority explicitly approves the following minimal
Event representation for this ticket:

- stable primary key;
- name;
- scheduled date/time;
- event type represented simply for the MVP;
- optional description;
- optional opponent information;
- optional map information;
- created timestamp;
- updated timestamp.

This approval is a shared/RED schema artifact and must be recorded and
implemented exactly within this boundary. It does **not** authorize:

- Unit relationships;
- EventParticipation;
- event ownership;
- event-manager relationships;
- permission relationships;
- Audits;
- generalized event types;
- multi-game abstractions;
- any additional persistent relationship or model.

Any additional persistent structure must be escalated before implementation.

## Main Success Scenario

1. The approved minimal Event representation exists in PostgreSQL.
2. Its migration applies successfully.
3. Controlled server-side verification can create and read an Event.
4. An unauthenticated visitor opens the Events area.
5. Existing Events are displayed.
6. The visitor selects an Event.
7. The Event detail view shows its basic information.
8. No authentication is required for public browsing.
9. Temporary verification Event data can be removed.

## Module Responsibilities

The Events module owns, for this ticket:

- Event identity;
- Event name;
- scheduled date/time;
- basic Event metadata;
- Event read/query behavior;
- public Event presentation.

It explicitly does **not** own yet:

- Unit participation requests;
- EventParticipation;
- participation approval;
- Unit authority;
- Event-manager permissions;
- Audit lifecycle;
- audit statistics;
- leaderboards;
- event-management UI;
- general Event create/edit/delete controls for application users.

## Required Work

Developer B should:

- create `src/modules/events/MODULE.md` from the canonical template;
- register the Events module in `docs/registry/MODULES.yaml`;
- introduce only the approved minimal Event Prisma representation;
- create the corresponding migration;
- preserve server-only Prisma access through an appropriate server-side module
  boundary;
- implement a public Events list;
- implement Event detail;
- display the name, date/time, event type, description when present, opponent
  when present, and map when present;
- provide useful empty and error states;
- use `/events` and `/events/[eventId]` when consistent with repository
  conventions.

The normal Event creation rule is that a domain-level Event creation path may
not create an Event scheduled in the past, provided this validator can be
added without establishing authorization or unrelated architecture.

Controlled server-side Event creation is permitted only for persistence
verification. It must not become public product behavior.

## Write Behavior

This ticket does not expose general Event create, edit, or delete controls to
application users. The MVP will later allow appropriately authorized Unit
managers to manage Events, but that permission model is being designed
separately.

Do not introduce a temporary rule that allows any authenticated user to create
Events. That would establish incorrect authorization behavior.

## Explicitly Out of Scope

- EventParticipation;
- participation requests;
- participation approval;
- Unit/Event relationships;
- Event manager authorization;
- event-management UI;
- Audit models;
- Audit UI;
- advanced event types;
- statistics;
- multi-game abstractions;
- generalized Event taxonomy;
- ownership or creator relationships;
- permission relationships.

## Contracts

Do not create speculative contracts. The Events module currently has no
justified need to consume Units. Event/Unit integration must wait for the
EventParticipation ticket.

If implementation identifies a genuine cross-module contract need, stop and
escalate it rather than depending on another module's private implementation.

## Scope

```yaml
scope:
  read:
    - AGENT_WORKFLOW.md
    - docs/DEVELOPMENT_STANDARD.md
    - docs/SYSTEM.md
    - docs/architecture.md
    - BACKLOG.md
    - README.md
    - docs/templates/MODULE_TEMPLATE.md
    - docs/templates/TICKET_TEMPLATE.md
    - docs/registry/MODULES.yaml
    - docs/registry/CONTRACTS.yaml
    - docs/registry/CAPABILITIES.yaml
    - docs/decisions/ADR-20260914-001.md
    - tickets/OPEN/**
    - prisma/schema.prisma
    - prisma/seed.ts
    - src/lib/prisma.ts
    - src/app/**
    - src/modules/**
    - package.json
  write:
    - tickets/OPEN/TKT-20260914-000005-001.md
    - src/modules/events/**
    - src/app/events/**
    - prisma/schema.prisma
    - prisma/migrations/**
    - docs/registry/MODULES.yaml
```

The `prisma/schema.prisma` and `prisma/migrations/**` entries are explicitly
limited to the approved minimal Event representation. No relationship,
additional model, or unrelated Prisma change is authorized. Before changing
shared persistence, Developer B must inspect open tickets for overlapping
schema work and coordinate if another active ticket changes the same boundary.

Read-scope expansions must be logged immediately with a concrete reason.
Write-scope expansions require explicit authorization and must be recorded
before use.

## Context Ledger

### Modules

- Declared: [events]
- Discovered: []
- Modified: []
- Created: []
- Externally modified: []

### Contracts

- Declared: []
- Discovered: []
- Modified: []
- Created: []

### Capabilities

- Declared: []
- Discovered: []

### Decisions

- Referenced:
  - ADR-20260914-001
  - TKT-20260914-000002-001 (shared backend foundation)
  - TKT-20260914-000003-001 (shared backend verification)
- Created: []

### Context Discoveries

| Artifact | Type | Reason discovered | Scope/log entry |
|---|---|---|---|
| TKT-20260914-000004-001 | active ticket | Coordinate the concurrent Units module work and avoid unauthorized Event/Unit coupling | 2026-09-14 / initial required context |
| prisma/schema.prisma | persistence implementation | Identify the existing schema and the approved location for the minimal Event representation | 2026-09-14 / initial required context |
| src/lib/prisma.ts | server boundary implementation | Preserve the existing server-only Prisma access pattern | 2026-09-14 / initial required context |
| docs/registry/MODULES.yaml | module registry | Confirm that Events is not yet registered and must be added by this ticket | 2026-09-14 / initial required context |

## Assumptions

| Statement | Status | Evidence or decision |
|---|---|---|
| The existing repository Prisma and PostgreSQL setup can support the approved minimal Event representation without unrelated persistence changes | unverified | Human-approved scope in this ticket; Developer B must validate before schema modification |
| The Events module does not need a Units contract for public browsing | unverified | Explicit Contracts section and current ticket boundary; confirm during implementation |
| `/events` and `/events/[eventId]` are compatible with repository routing conventions | unverified | Suggested routes; confirm against existing app structure before implementation |
| A local/domain-level past-date validator can be added without deciding authorization or unrelated architecture | unverified | Event Time Rule; escalate if implementation requires a broader design decision |
| Controlled server-side Event creation can be performed and cleaned up without exposing product write behavior | unverified | Main Success Scenario and Write Behavior; verify during implementation |

## Acceptance Criteria

- [ ] `src/modules/events/MODULE.md` exists and follows `docs/templates/MODULE_TEMPLATE.md`.
- [ ] Events module is registered in `docs/registry/MODULES.yaml`.
- [ ] Module ownership and non-ownership are explicit.
- [ ] The approved minimal Event Prisma representation exists and no unauthorized relationship or model is introduced.
- [ ] The corresponding migration exists and applies to a clean development database.
- [ ] Controlled server-side Event creation and read succeed, followed by cleanup of temporary verification data.
- [ ] Prisma access remains server-only.
- [ ] Public Events list works for an unauthenticated visitor.
- [ ] Public Event detail works for an unauthenticated visitor.
- [ ] Required Event metadata is displayed, with optional fields shown when present.
- [ ] Useful empty and error states are provided.
- [ ] Public browsing works without login.
- [ ] EventParticipation is not introduced.
- [ ] No Unit/Event relationship is introduced.
- [ ] No authorization behavior is invented.
- [ ] No Audit behavior is introduced.
- [ ] No speculative contract is created.
- [ ] `npm run lint` passes.
- [ ] `npm run type-check` passes.
- [ ] `npm run build` passes.

## Verification

The eventual implementation must verify at least:

- Prisma validation;
- Prisma client generation;
- a clean migration against PostgreSQL using the existing Docker/PostgreSQL
  verification approach where useful;
- controlled temporary Event creation;
- Event read;
- public Events list;
- public Event detail;
- temporary Event cleanup;
- `npm run lint`;
- `npm run type-check`;
- `npm run build`.

At completion, record each check as PASS, FAIL, or ENVIRONMENT-BLOCKED with
the command or walkthrough, expected result, actual result, and next action
for failures or blockers.

## Collaboration / Conflict Notes

This ticket is expected to execute concurrently with:

- Developer A's Units hierarchy ticket;
- Lead's Unit authority/invite architecture ticket.

Developer B is expected to be the only developer intentionally changing the
product-domain portion of `prisma/schema.prisma` during this first parallel
round. Before modifying the schema, inspect open tickets for conflicting
schema work. If another active ticket is changing overlapping persistence
structure, stop and coordinate before proceeding.

Developer B must avoid modifying Unit persistence, authentication, or active
architecture-design artifacts. Changes to `docs/registry/MODULES.yaml` must
remain limited to registering Events and must account for concurrent registry
work before editing.

## Stop Conditions

Stop and escalate if implementation requires deciding:

- Event-manager permissions;
- Event creator ownership;
- Unit/Event relationships;
- EventParticipation;
- participation statuses;
- Audit relationships;
- cross-module contracts;
- generalized Event taxonomy;
- additional persistent models or relationships;
- multi-game abstractions.

## Boundary and Conflict Review

- Shared artifacts checked: `tickets/OPEN/`, `tickets/COMPLETED/`, `docs/decisions/`, `docs/registry/`, `prisma/schema.prisma`
- Boundary change required: yes; human-approved minimal Event schema and new Events module boundary are recorded in this ticket
- BCR: none; the approved schema scope is explicitly authorized by the request
- Scope-growth review: any persistence, authorization, relationship, contract, or taxonomy expansion requires escalation and a separate approval path

## Completion Record

Status: NOT STARTED
Completed: N/A
Implementation commit: N/A
Pull request: N/A

### Result

Pending implementation by Developer B.

### Files Changed

- `tickets/OPEN/TKT-20260914-000005-001.md`

### Documentation Updated

- None; implementation documentation is pending Developer B's work.

### Registries, Changelogs, and Decisions

- Registries: pending implementation.
- Changelogs: pending implementation.
- BCRs: none.
- ADRs: none created; ADR-20260914-001 referenced.

### Deviations and Known Limitations

- This ticket creation authorizes no implementation and leaves the ticket in `READY`.